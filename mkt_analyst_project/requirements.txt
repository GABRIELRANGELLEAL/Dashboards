# =========================
# Dependências principais
# =========================
pandas # manipulação de dados
streamlit           # framework web
openai>=1.0.0       # cliente oficial OpenAI (novo SDK)
python-dotenv       # carregar variáveis de ambiente (.env)
tabulate            # usado pelo pandas.to_markdown e exportações
requests            # chamadas HTTP (Facebook API, etc.)

# =========================
# Dependências opcionais
# (inclua se realmente usar
# outras partes do app)
# =========================
pydub               # manipulação de áudio
streamlit-webrtc    # streaming de áudio/vídeo em tempo real
